import {TextStyle} from 'react-native';

const fonts = {
  h1: {
    fontSize: 24,
    fontWeight: 'bold',
  } as TextStyle,
  h2: {
    fontSize: 20,
    fontWeight: 'bold',
  } as TextStyle,
  body: {
    fontSize: 16,
  } as TextStyle,
  caption: {
    fontSize: 12,
  } as TextStyle,
};

export default fonts;
